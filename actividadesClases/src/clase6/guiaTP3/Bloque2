package clase6.guiaTP3;

import interfaces.ColaTDA;
import interfaces.ColaPrioTDA;
import implementacionDinamica.colaDinamica.ColaDinamica;
import implementacionDinamica.colaPrioridadDinamica.ColaPrioridadDinamica;

public class Bloque2 {

    public void ejercicios() {
        // 1. Fila del Cajero
        ColaTDA banco = new ColaDinamica();
        banco.InicializarCola();

        // 2. Impresora del Laboratori
        ColaTDA impresora = new ColaDinamica();
        impresora.InicializarCola();
        impresora.Acolar(1);
        impresora.Acolar(2); 

        // 3. Guardia Hospital (Triage)
        ColaPrioTDA guardia = new ColaPrioridadDinamica();
        guardia.InicializarCola();
        guardia.AcolarPrioridad(111, 1);
        guardia.AcolarPrioridad(222, 100); 

        // 4. Examen Final
        ColaPrioTDA finalProg = new ColaPrioridadDinamica();
        finalProg.InicializarCola();
        finalProg.AcolarPrioridad(800, 5); 
        finalProg.AcolarPrioridad(830, 10); 

        // 5. Buffet Facultad
        ColaTDA buffet = new ColaDinamica();
        buffet.InicializarCola();
        buffet.Acolar(1)
        Buffet.Acolar(2)
        // Asi me doy cuenta xq al desacolar salen en orden de llegada

        // 6. Procesador de Tareas
        ColaPrioTDA cpu = new ColaPrioridadDinamica();
        cpu.InicializarCola();
        cpu.AcolarPrioridad(1, 50); 
        cpu.AcolarPrioridad(2, 10); 

        // 7. Embarque Aerolíneas
    
        ColaPrioTDA avion = new ColaPrioridadDinamica();
        avion.InicializarCola();
        avion.AcolarPrioridad(10, 3); /
        avion.AcolarPrioridad(20, 3); 

        // 8. Tickets IT
        ColaPrioTDA soporte = new ColaPrioridadDinamica();
        soporte.InicializarCola();
        soporte.AcolarPrioridad(1, 999); 
        soporte.AcolarPrioridad(2, 0);   
    }
}